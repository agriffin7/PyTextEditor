This is a very simple text editor application writen in python. It uses the built-in tkinter library for the graphical interfacing. 
This program can accept text into the text box and be able to: Save text to a file, Open a file and retreive text, and allow new text instances to be created.
